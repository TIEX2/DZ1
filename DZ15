        static void Main(string[] args)
        {
            string password = "StarWars";
            string secretMessenge = "May the Force be with you";
            int tryCount = 3;
            int endTheProgram = 0;
            string userInput;

            Console.WriteLine("Введите пароль, чтобы получить секретное сообщение! У вас всего 3 попытки.");

            while(tryCount-- > endTheProgram)
            {
                Console.Write("Пароль: ");
                userInput = Console.ReadLine();

                if(userInput == password)
                {
                    Console.WriteLine(secretMessenge);
                    break;
                }
                else
                {
                    Console.WriteLine("Неверный пароль. Попробуйте снова!");
                }
            }

            if(tryCount < endTheProgram)
            {
                Console.WriteLine("Количество попыток исчерпано!");
            }
        }
